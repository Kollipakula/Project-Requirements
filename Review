  
Please do the following:

1. Run the complete backend Maven test suite using the existing JUnit/Spring Boot test setup.

2. Inspect every test failure and determine whether it is:
   - a real production-code failure,
   - a test setup/configuration failure,
   - an unnecessary Mockito stubbing failure,
   - or a test discovery/execution problem.

3. Fix ONLY the test configuration/test code necessary to make the existing tests valid.
   Do NOT modify PaymentBatchController.java, PaymentBatchService.java, repositories, DTOs, database logic, or any production behavior just to make tests pass.

4. For PaymentBatchServiceTest:
   - inspect the setUp() method,
   - identify the unnecessary Mockito stubbings causing UnnecessaryStubbingException,
   - move stubbing into only the tests that actually require it, or otherwise correct the test setup without weakening Mockito strictness unnecessarily.

5. For TrainingApplicationTests:
   - determine why Spring Boot cannot find @SpringBootConfiguration,
   - correct the test package/configuration so the test can locate the existing Spring Boot application class,
   - do not create a duplicate application class.

6. For PaymentBatchControllerTest:
   - determine why the test source exists but no Surefire report/executed test is being produced,
   - verify that the test is actually discovered and executed by Maven.
   - If the test itself is invalid, explain exactly why before changing it.

7. After making only the necessary test-side corrections, run:
   mvn test

8. Report the final result as:
   - Tests run
   - Passed
   - Failed
   - Errors
   - Skipped

9. Also provide a clear list of every file you changed and why.
