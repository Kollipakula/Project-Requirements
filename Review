  Before making ANY code changes, I want you to perform a complete diagnostic review of the payroll payment flow.

Do NOT modify, create, delete, or auto-fix any files yet.

The current intended API contract is:

POST /payments/payroll/draft
POST /payments/payroll/submit
POST /payments/payroll/{batchId}/submit
POST /payments/payroll/{batchId}

Please inspect the actual frontend and backend code and verify whether the following files really need changes:

1. frontend/public/index.html
2. frontend/src/services/paymentService.js
3. frontend/src/services/api.js
4. frontend/package.json
5. backend PaymentBatchController.java
6. backend PaymentBatchService.java
7. application.properties
8. application-dev.properties
9. data-dev.sql

For each file, tell me:

- Whether it actually needs to be changed
- The exact reason
- The current relevant code/mapping
- What the correct expected behavior should be
- Whether the problem is frontend, backend, database, proxy/CORS, or configuration
- Do not suggest unnecessary changes

Also verify the following specifically:

1. Confirm the exact Spring controller mappings for:
   POST /payments/payroll/draft
   POST /payments/payroll/submit

2. Confirm whether the frontend is calling exactly those same paths.
   There must NOT be an accidental /payment/payroll or /payroll path.

3. Inspect api.js and paymentService.js and determine whether Axios is using:
   - relative URLs through the CRA proxy, OR
   - an explicit http://localhost:8080 base URL.
   Identify which approach the project is currently designed to use.

4. Inspect package.json and index.html and determine whether the frontend is CRA or Vite.
   Check whether index.html is using the correct bootstrap mechanism for the actual project.

5. Check the X-User-Id flow.
   The frontend currently appears to send X-User-Id: 1.
   Verify how that value is obtained from authService/session and how the backend resolves it.

6. Check the database/user data.
   Verify whether user ID 1 actually exists in the database used by the running Spring application.
   Do not assume that data-dev.sql is automatically being loaded.

7. Explain why Swagger receives:
   400 User not found: 1

   but the browser request appears to receive:
   404 or 500.

8. Determine whether CORS is actually the problem or whether the frontend is supposed to use the configured proxy instead.

9. Test the backend endpoints if possible without modifying the code.
   Test both:
   POST /payments/payroll/draft
   POST /payments/payroll/submit

   Use a valid request body and X-User-Id value only if such a valid user exists.

10. Check whether the 404 is actually coming from Spring or from the frontend development server/proxy.

At the end, give me a diagnostic report in this exact structure:

A. BACKEND MAPPING STATUS
B. FRONTEND ENDPOINT STATUS
C. FRONTEND BOOTSTRAP STATUS
D. PROXY/CORS STATUS
E. X-USER-ID STATUS
F. DATABASE/USER STATUS
G. DRAFT API TEST
H. SUBMIT API TEST
I. FILES THAT ACTUALLY NEED CHANGES
J. FILES THAT DO NOT NEED CHANGES
K. EXACT ROOT CAUSE(S)

Most importantly:
DO NOT MODIFY ANY FILES.
DO NOT "fix" anything yet.
I only want the verified diagnosis and testing results first.
