As of now, we have only created the backend folder structure and have not started the backend implementation yet. Before we start implementing the backend, we want to finalize the database schema for the entire project because different team members will be working on different components and there will be relationships between them.

I have attached/provided our current DB Schema PDF. Please review the complete database schema against the existing frontend implementation in this repository.

Please check:

Whether all frontend features/components are properly supported by the database.
Whether all required tables, columns, primary keys and foreign keys are present.
Whether the relationships between tables are correct (especially one-to-many relationships such as batch → transactions/payments).
Whether the schema correctly supports our payment and payroll flows.
Whether it supports saved drafts.
Whether it supports currency and currency conversion requirements.
Whether it supports our three roles: Operator, Approver and Viewer.
Operator can create/edit payments.
Approver can approve payments.
Viewer can only view/read and cannot create, edit or approve.
Our approval requirement has now been clarified by the SME:
The number of approval levels depends on the payment amount.
We can have a maximum of two approval levels.
We can have around five approvers, and for a payment requiring two approvals, any two eligible approvers can approve.
For a payment requiring one approval, any one eligible approver can approve.
A user can be both Operator and Approver, but the person who initiated a particular payment/batch cannot approve that same payment/batch.

Also verify whether the database design can correctly determine which users are eligible to approve a particular payment without unnecessarily making the design complex.

Important: Do not modify or generate implementation code yet. This is only a database and architecture review. Please give me:

What is already correct.
What needs to be changed.
Any missing tables/columns/relationships.
Any unnecessary tables or over-complex design.
Your recommended final database structure.
A clear table-by-table explanation of the changes.

Please base your review on the actual files in this repository, especially the existing frontend implementation and the DB Schema PDF, rather than making assumptions.
