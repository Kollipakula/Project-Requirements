We are implementing the Create Payroll backend feature in an existing Spring Boot project.

Before making any changes, inspect the existing entity classes:
- PaymentBatch.java
- BatchPayment.java

Also inspect the related entities only if necessary to verify the relationships:
- Account.java
- Currency.java
- User.java

First, VERIFY the following. Do not modify anything yet:

1. Verify the primary key / ID mapping in PaymentBatch.java.
2. Verify the primary key / ID mapping in BatchPayment.java.
3. Verify that PaymentBatch is the PARENT entity for BatchPayment.
4. Verify that one PaymentBatch can have MANY BatchPayment records.
5. Verify that BatchPayment has a MANY-TO-ONE relationship back to PaymentBatch.
6. Verify that the @JoinColumn used by BatchPayment correctly references the PaymentBatch ID column.
7. Verify that the relationship will allow us to save multiple employee payment rows under one payroll/payment batch.
8. Verify that the executionDate field in PaymentBatch is correctly mapped to the execution_date database column.
9. Check for any obvious JPA mapping problems that could prevent the entities from being persisted.

IMPORTANT:
- Do NOT redesign the entities.
- Do NOT create an Employee entity.
- Do NOT modify approval-related entities or files.
- Do NOT modify PaymentBatch.java or BatchPayment.java unless there is an actual compilation/JPA mapping error that must be fixed.
- Do NOT create services, controllers, DTOs, or database tables yet.

After verification, implement ONLY these two repository interfaces:

1. PaymentBatchRepository.java
   - package: com.axess.payment.repository
   - extends JpaRepository<PaymentBatch, Long>

2. BatchPaymentRepository.java
   - package: com.axess.payment.repository
   - extends JpaRepository<BatchPayment, Long>

Do not add unnecessary custom query methods at this stage.

Finally, compile the backend and report:
- whether the entity relationships are correct
- what changes, if any, were necessary
- whether compilation succeeded
- which files were changed




*********************************
Before implementing any new code, carefully inspect the existing PaymentBatch.java and BatchPayment.java entities and verify their JPA relationship mappings against the existing project structure.

Do NOT modify any files yet.

Verify specifically:

1. PaymentBatch -> BatchPayment
   - Confirm that one PaymentBatch can have many BatchPayment records.
   - Confirm the mappedBy value is correct.
   - Confirm the JoinColumn/batch_id relationship is correct.
   - Confirm cascade/orphanRemoval settings if present and whether they are appropriate.

2. BatchPayment -> PaymentBatch
   - Confirm that each BatchPayment belongs to exactly one PaymentBatch.
   - Confirm @ManyToOne and @JoinColumn(name = "batch_id") are correct.

3. PaymentBatch relationships with existing entities:
   - User
   - Account/source account
   - Currency
   Verify that the referenced entity types, JoinColumn names, nullable settings, and relationship annotations match the existing project/database design.

4. Check for bidirectional relationship problems, JSON serialization issues, Hibernate lazy-loading issues, duplicate mappings, or incorrect column names.

5. Inspect the existing project entities and repositories before making any conclusion.

Do not create Service, Controller, DTO, or additional Repository code yet.

At the end, give me:
- Relationship verification result
- Any incorrect mappings found
- Exact files that would need changes, if any
- Confirmation that the current entities are safe to use for the next Service-layer implementation

Do not make changes unless I explicitly approve them.

--------------------------------------------------Now that the JPA entity relationships have been verified and the PaymentBatchRepository and BatchPaymentRepository are implemented, implement the service layer for PaymentBatch and BatchPayment.

Now that the JPA entity relationships have been verified and the PaymentBatchRepository and BatchPaymentRepository are implemented, implement the service layer for PaymentBatch and BatchPayment.

IMPORTANT:
- Inspect the existing project structure, entities, repositories, DTOs, services and controllers before making changes.
- Do not redesign or modify the existing entity mappings unless absolutely required for compilation.
- Do not create duplicate classes.
- Follow the existing coding style and patterns used by the other services in this project.

Implement PaymentBatchService.java with the following responsibilities:

1. CREATE / SAVE PAYMENT BATCH
   - Create a PaymentBatch with its required fields.
   - Support multiple BatchPayment child records under the same PaymentBatch.
   - Since there is no cascade/orphanRemoval, explicitly save the PaymentBatch first and then save each BatchPayment using BatchPaymentRepository.
   - Ensure the manually assigned IDs are handled consistently with the existing entity design.
   - Set the appropriate initial status for a newly saved draft.

2. LOAD PAYMENT BATCH
   - Load a PaymentBatch by its ID.
   - Load its associated BatchPayment records.
   - Return the batch together with its child payment rows.

3. UPDATE PAYMENT BATCH
   - Allow an existing draft batch to be edited.
   - Update existing BatchPayment rows.
   - Handle newly added child rows.
   - Do not delete existing child rows unless the existing project design clearly requires it.

4. SUBMIT FOR APPROVAL
   - A completed PaymentBatch should be able to go directly from creation to submission for approval.
   - Saving as a draft must NOT be mandatory before submission.
   - Validate that required batch/payment information is present.
   - Change the batch status to the appropriate "pending approval" status used by the existing project.
   - Do not implement the actual multi-level approval processing yet; only prepare the batch for the existing approval workflow.

5. TRANSACTION SAFETY
   - Use @Transactional where appropriate.
   - Ensure that if saving the parent succeeds but saving a child fails, the transaction rolls back appropriately.

6. ERROR HANDLING
   - Use the existing project's exception/error-handling approach.
   - Do not introduce a new exception framework unless the project already uses one.

7. DO NOT implement the controller yet.
8. DO NOT change the frontend yet.
9. DO NOT modify unrelated services or entities.

Before changing files, inspect:
- PaymentBatch.java
- BatchPayment.java
- PaymentBatchRepository.java
- BatchPaymentRepository.java
- existing Service classes
- PaymentRequest.java
- existing approval-related entities/services
- existing status fields/enums

After implementation:
- compile the backend
- report which files were changed
- explain the implemented methods
- identify any assumptions about status values, IDs, or DTO structure
- do not make unrelated changes.
---------------------------------------------
Before making any changes, inspect the existing PaymentBatch and BatchPayment entities AND the database schema/configuration.

We want to simplify ID handling by using JPA/database-generated IDs instead of the current manual MAX(ID) + 1 approach.

Verify:

1. PaymentBatch.batchId
   - Is it the @Id field?
   - What is its database column?
   - Is that database column configured as IDENTITY/AUTO_INCREMENT/generated by the database?

2. BatchPayment.batchPaymentId
   - Is it the @Id field?
   - What is its database column?
   - Is that database column configured as IDENTITY/AUTO_INCREMENT/generated by the database?

3. Check the actual database schema/migrations/schema.sql/data.sql if present.

4. Check whether using:
   @GeneratedValue(strategy = GenerationType.IDENTITY)
   would be compatible with the existing database.

5. Check all relationships:
   PaymentBatch -> BatchPayment
   BatchPayment -> PaymentBatch
   and verify the foreign-key/join-column configuration.

If the database supports generated IDs:
- Update the entities to use @GeneratedValue(strategy = GenerationType.IDENTITY).
- Remove the manual ID-generation logic from PaymentBatchService.
- Do not make unrelated changes.
- Keep the existing business logic unchanged.

If the database does NOT support generated IDs:
- Do NOT change anything.
- Explain exactly what prevents us from using @GeneratedValue.

After making changes, compile the backend with Maven and report the result.

Do not implement controllers, DTOs, or other layers yet.
-------------------------------------------
We are about to implement the DTO and Controller layer for the Create Payroll / Payment Batch feature.

Before creating or modifying ANY files, inspect the existing frontend Create Payroll implementation and the existing backend entities/services.

I want you to ONLY ANALYZE and REPORT. Do not make any code changes.

Specifically inspect the Create Payroll frontend page/component and identify:

1. What fields are entered by the corporate user.

2. What frontend validation is performed before submission.

3. What exact payload/object is created when the user clicks:
   - Save Draft
   - Submit for Approval

4. Show the structure of the payload being sent to the backend.

5. For the source account:
   - Does the frontend send an account ID?
   - An account number?
   - An account name?
   - An entire account object?
   Show the exact field name and value type.

6. For currency:
   - Does the frontend send currency ID?
   - Currency code such as INR/USD?
   - Currency name?
   - An entire currency object?
   Show the exact field name and value type.

7. For the payment/employee rows, identify exactly what is sent for each BatchPayment:
   - employee ID
   - employee name
   - beneficiary account/account number
   - bank code
   - amount
   - any other fields

8. Identify whether executionDate is sent and what format it uses.

9. Identify whether totalAmount and totalTransactions are calculated by the frontend or expected to be calculated by the backend.

10. Identify whether batchId or batchPaymentId are sent from the frontend when creating a new batch.

11. Check whether the frontend currently stores Save Draft data only in localStorage or whether it already calls a backend API.

12. Compare the frontend payload with the current backend entities:
   - PaymentBatch.java
   - BatchPayment.java
   - PaymentBatchService.java

13. Identify any mismatch between frontend field names/types and backend field names/types.

IMPORTANT:
- Do NOT create DTOs.
- Do NOT create controllers.
- Do NOT modify frontend or backend code.
- Do NOT make assumptions.
- Inspect the actual code and report what is currently happening.
- If something cannot be determined from the existing code, explicitly say "Cannot determine from current code."

At the end, provide:

A. Current Create Payroll frontend payload
B. Save Draft payload
C. Submit for Approval payload
D. Field-by-field mapping to PaymentBatch
E. Field-by-field mapping to BatchPayment
F. Any mismatches/problems we need to address before creating DTOs
-----------------------------------
Before we implement DTOs for the Payment Batch module, I want you to do a complete codebase review first.

IMPORTANT:
Do NOT modify any code yet.
Do NOT create DTOs yet.
Do NOT refactor anything yet.

I want you to inspect the existing frontend and backend implementation and give me a clear assessment of what information is required for the DTO design.

Focus specifically on the Payment Batch flow and inspect:

BACKEND:
- PaymentBatch entity
- BatchPayment entity
- PaymentBatchRepository
- BatchPaymentRepository
- PaymentBatchService
- Any related approval entities/repositories/services
- PaymentBatchController and BatchPaymentController, if present
- Existing request/response models or DTOs, if any

FRONTEND:
- Payment batch listing
- Batch details page
- Payment/transaction details
- Approval page/approval workflow
- Any screens where batch information, transactions, approval levels, status, remarks, balances, etc. are displayed or submitted
- API calls made by the frontend for these screens

I previously asked you to scan the frontend and identify what the Payment Batch/Approval UI needs. Use that understanding together with the current backend implementation.

I specifically want you to answer:

1. What data does the frontend actually need from PaymentBatch?

2. What data does the frontend actually need from BatchPayment?

3. Which entity fields are currently being exposed directly but should instead be exposed through DTOs?

4. Which fields should be included in:
   - PaymentBatchResponseDTO
   - BatchPaymentResponseDTO
   - PaymentBatchRequestDTO
   - BatchPaymentRequestDTO
   - Approval-related DTOs, if required

5. For every proposed DTO field, tell me:
   - Entity field it comes from
   - Database column
   - Data type
   - Why the frontend needs it
   - Whether it is required or optional

6. Check the entity relationships carefully:
   - PaymentBatch → BatchPayment
   - PaymentBatch → User
   - PaymentBatch → Account
   - PaymentBatch → Currency
   - PaymentBatch → BatchApproval
   - BatchPayment → PaymentBatch

   Tell me which relationships should be represented in DTOs as IDs, simple objects, nested DTOs, or not exposed at all.

7. Check for possible problems caused by directly returning JPA entities:
   - Lazy loading
   - Hibernate serialization
   - Circular references
   - @JsonIgnore
   - unnecessary database data being exposed
   - security/data exposure concerns

8. Check the current PaymentBatchService and repositories and tell me whether they already provide everything required for the frontend or whether any repository/service changes will be needed after introducing DTOs.

9. Most importantly, identify the MINIMUM DTO structure required for our current frontend.
   I don't want unnecessary fields just because they exist in the entity.

10. Give me a final recommended DTO design and explain the data flow:

Frontend
→ Controller
→ Request DTO
→ Service
→ Entity
→ Repository

and

Repository
→ Entity
→ Service
→ Response DTO
→ Controller
→ Frontend

11. Also identify exactly which existing files/classes will need to be changed when we implement the DTOs.

12. Do NOT write the implementation yet.
Just give me the analysis, recommended DTO classes, their fields, and the files that need to change.

The goal is to make sure the DTO design matches our CURRENT frontend requirements and existing backend architecture before we start coding.


-----------------------------------
Before implementing any DTOs, I want you to do one final read-only review of the current project and give me the exact DTO contract we should implement.

You have already reviewed the frontend and identified the current mismatches between the frontend payload and the backend PaymentBatch/BatchPayment entities.

I have now confirmed that the current backend has:
- PaymentBatch entity
- BatchPayment entity
- PaymentBatchRepository
- BatchPaymentRepository
- PaymentBatchService with saveDraft, createAndSubmit, updateDraft, submitForApproval and loadBatchWithPayments
- Existing validation and PaymentBatch -> BatchPayment relationship

IMPORTANT:
1. Do NOT modify any code.
2. Do NOT create DTO classes yet.
3. Do NOT modify entities, repositories, services, controllers, database schema, or frontend.
4. This must be a READ-ONLY analysis.
5. Base your answer on the actual current frontend and backend code, not assumptions.
6. If you need to inspect any existing file to make the decision, tell me exactly which file/class you need me to provide. Do not guess.

I want you to answer these questions:

1. Is the current backend foundation ready for DTO implementation?
2. What exact DTO classes are required RIGHT NOW for the current Create Payroll/Draft/Submit flow?
3. Give the final field-by-field structure for each DTO:
   - DTO class name
   - field name
   - Java type
   - required/optional
   - frontend field it maps to
   - backend entity field it maps to
   - whether it is request or response only
4. Clearly separate:
   - Request DTOs
   - Response DTOs
   - DTOs that are NOT needed yet
5. Show the exact JSON request payload expected from the current frontend for:
   - Save Draft
   - Create/Submit
   - Update Draft
6. Show the exact JSON response payload the frontend should receive.
7. Explain how Account and Currency should be represented:
   - what frontend sends
   - what the DTO accepts
   - how the service should eventually map it to Account/Currency entities
8. Decide the canonical status values for the DTO contract. Specifically resolve the current SUBMITTED vs PENDING mismatch. Explain which value should be used and why based on the existing code/frontend.
9. Explain how PaymentBatch -> BatchPayment should be represented in request and response DTOs.
10. Confirm whether sourceAccount, currency, createdByUser, batchApprovals and other related JPA entities should be exposed directly. If not, specify the safe DTO representation.
11. Confirm whether we need any repository changes immediately for DTO implementation, or whether the current repositories are sufficient.
12. Confirm whether the current PaymentBatchService business logic can remain mostly unchanged and simply be adapted to DTO input/output.
13. Identify any issue that MUST be fixed before DTO implementation starts.

Finally, give me a single "FINAL APPROVED DTO CONTRACT" section containing only the DTO classes and their fields that we should actually implement now.

Do not provide implementation code yet. I want to approve the contract first.

------------------------------------------------The DTO contract shown in the previous review is FINAL and APPROVED.

Now implement the DTO layer exactly according to that approved contract.

IMPORTANT: Before changing anything, inspect the ACTUAL current code in:
- PaymentBatch.java
- BatchPayment.java
- User.java
- Account.java
- Currency.java
- PaymentBatchService.java
- PaymentBatchController.java
- existing DTOs under com.axess.payment.dto

Do not guess entity field names. Use the actual entity definitions.

RULES:
1. Do not redesign entities.
2. Do not change the database schema.
3. Do not change frontend code.
4. Do not create approval DTOs yet.
5. Do not make unrelated changes.
6. Do not refactor unrelated code.
7. Preserve the existing business logic and validation.
8. Do not change the existing entity relationships.
9. Do not expose JPA entities directly from controller responses.

==================================================
DTOs TO IMPLEMENT
==================================================

Create exactly these 7 DTO classes:

REQUEST:
1. PaymentBatchRequestDTO
2. BatchPaymentRequestDTO

RESPONSE:
3. PaymentBatchResponseDTO
4. BatchPaymentResponseDTO
5. SimpleUserDTO
6. SimpleAccountDTO
7. SimpleCurrencyDTO

Use the EXACT fields and Java types from the FINAL APPROVED DTO CONTRACT from the previous review.

Do not add extra fields.
Do not remove approved fields.
Do not rename approved fields.

==================================================
REQUEST DTO CONTRACT
==================================================

PaymentBatchRequestDTO:

- action: String
- batchId: Long
- batchReference: String
- paymentType: String
- executionDate: LocalDate
- sourceAccountId: Long
- sourceAccountName: String
- currencyCode: String
- totalAmount: BigDecimal
- description: String
- payments: List<BatchPaymentRequestDTO>

BatchPaymentRequestDTO:

- batchPaymentId: Long
- employeeId: String
- employeeName: String
- beneficiaryAccount: String
- bankCode: String
- amount: BigDecimal

==================================================
RESPONSE DTO CONTRACT
==================================================

PaymentBatchResponseDTO:

- batchId: Long
- batchReference: String
- paymentType: String
- status: String
- executionDate: LocalDate
- totalAmount: BigDecimal
- totalTransactions: Integer
- createdAt: LocalDateTime
- submittedAt: LocalDateTime
- approvedAt: LocalDateTime
- rejectedAt: LocalDateTime
- processedAt: LocalDateTime
- initialBalance: BigDecimal
- finalBalance: BigDecimal
- description: String
- createdBy: SimpleUserDTO
- sourceAccount: SimpleAccountDTO
- currency: SimpleCurrencyDTO
- payments: List<BatchPaymentResponseDTO>

BatchPaymentResponseDTO:

- batchPaymentId: Long
- employeeId: String
- employeeName: String
- beneficiaryAccount: String
- bankCode: String
- amount: BigDecimal

SimpleUserDTO:

- userId: Long
- fullName: String

SimpleAccountDTO:

- accountId: Long
- accountNumber: String
- accountName: String

SimpleCurrencyDTO:

- currencyCode: String
- currencyName: String

==================================================
DTO DESIGN
==================================================

DTOs must remain separate from JPA entities.

Do NOT:
- annotate DTOs with @Entity
- put JPA relationships in DTOs
- expose User, Account, Currency, PaymentBatch or BatchPayment entities directly
- put repository logic in DTOs
- put service/business logic in DTOs

Use the simple DTOs for nested response objects:

PaymentBatchResponseDTO.createdBy
        -> SimpleUserDTO

PaymentBatchResponseDTO.sourceAccount
        -> SimpleAccountDTO

PaymentBatchResponseDTO.currency
        -> SimpleCurrencyDTO

PaymentBatchResponseDTO.payments
        -> List<BatchPaymentResponseDTO>


==================================================
BACKEND DTO MAPPING
==================================================

After creating the DTOs, integrate them into the existing
PaymentBatchController / PaymentBatchService flow.

The intended architecture is:

Frontend Request DTO
        ↓
Controller
        ↓
PaymentBatchService
        ↓
PaymentBatch / BatchPayment entities
        ↓
Repository

and:

Repository
        ↓
PaymentBatch / BatchPayment entities
        ↓
DTO mapping
        ↓
PaymentBatchResponseDTO
        ↓
Controller
        ↓
Frontend


IMPORTANT:

Preserve the existing PaymentBatchService business logic.

Existing flows that must continue to work:

- saveDraft()
- createAndSubmit()
- updateDraft()
- submitForApproval()
- loadBatchWithPayments()

Do not rewrite their business rules unnecessarily.

Do not remove existing validation.

Do not change how DRAFT/PENDING/APPROVED/REJECTED/PROCESSED
workflow logic currently works.

==================================================
STATUS RULE
==================================================

Keep action separate from persisted workflow status.

Request:
- action may indicate DRAFT or SUBMIT.

Persisted backend status must continue using the existing vocabulary:

- DRAFT
- PENDING
- APPROVED
- REJECTED
- PROCESSED

Do NOT introduce SUBMITTED as a backend persisted status.

The current frontend may still contain "SUBMITTED", but do not add
SUBMITTED to the backend status model as part of this task.


==================================================
ACCOUNT / CURRENCY / USER MAPPING
==================================================

Inspect the actual entity fields first.

For PaymentBatch relationships:

PaymentBatch.sourceAccount
        -> SimpleAccountDTO

PaymentBatch.currency
        -> SimpleCurrencyDTO

PaymentBatch.createdByUser
        -> SimpleUserDTO

Map the actual entity fields to the approved DTO fields.

If the actual entity field names differ from the approved DTO names,
use the appropriate mapping based on the actual entity structure.

Do NOT modify the entity just to make the DTO mapping easier.


==================================================
BATCH PAYMENT MAPPING
==================================================

Map:

BatchPayment.batchPaymentId
BatchPayment.employeeId
BatchPayment.employeeName
BatchPayment.beneficiaryAccount
BatchPayment.bankCode
BatchPayment.amount

to BatchPaymentResponseDTO.

For request mapping, convert:

BatchPaymentRequestDTO
        ->
BatchPayment entity

and associate the child entity with the correct PaymentBatch.


==================================================
IMPORTANT UPDATE-DRAFT RULE
==================================================

Preserve the existing updateDraft behavior.

If batchPaymentId is supplied:
- treat it as an existing child row
- update the corresponding existing BatchPayment

If batchPaymentId is null:
- treat it as a new payment row

Do not break the existing ownership/security check for child rows.

Do not introduce unrelated update behavior.


==================================================
VALIDATION
==================================================

Preserve all existing service-level validation.

If the project already uses Jakarta Bean Validation annotations,
use the existing project convention for required DTO fields.

Do not introduce a new validation framework or dependency.

Do not move business validation from the service into DTOs.


==================================================
DO NOT CREATE YET
==================================================

Do NOT create:

- ApprovalQueueItemDTO
- BatchApprovalActionRequestDTO
- BatchApprovalResponseDTO
- any other approval DTOs
- mapper classes unless absolutely necessary for this DTO integration
- new entities
- new repositories
- database migrations


==================================================
SCOPE CONTROL
==================================================

Only modify files that are genuinely required for:

1. Creating the 7 approved DTOs.
2. Mapping request DTOs to existing entities.
3. Mapping existing entities to response DTOs.
4. Updating the relevant PaymentBatchController/Service signatures
   only where necessary to use the DTOs.

Do NOT refactor unrelated code.

Do NOT modify unrelated controllers, services, repositories,
entities, or existing DTOs.


==================================================
FINAL VERIFICATION
==================================================

After implementation:

1. Compile/build the backend.
2. Check for Java compilation errors.
3. Check imports.
4. Check package names.
5. Check every DTO field against the approved contract.
6. Verify that no JPA entity is directly returned by the controller.
7. Verify that Account/Currency/User are represented by their
   corresponding Simple DTOs.
8. Verify that existing PaymentBatchService validation and workflow
   behavior is preserved.
9. Verify that backend statuses remain:
   DRAFT, PENDING, APPROVED, REJECTED, PROCESSED.
10. Verify that SUBMITTED was not introduced as a backend status.

Finally, report:

1. Files created.
2. Files modified.
3. Exact mapping decisions made because actual entity field names
   differed from DTO field names.
4. Whether the project compiles successfully.
5. Any remaining compilation/runtime issue.
6. Confirm whether any unrelated files were changed.

STOP after DTO implementation, DTO mapping, and verification.

Do not proceed to frontend changes or approval workflow changes.


-----------------------------+-----------------
The DTO implementation has already been applied. Do NOT restart the implementation and do NOT create duplicate DTOs.

Now perform a careful READ-ONLY REVIEW of the current changes against the FINAL APPROVED DTO CONTRACT below.

IMPORTANT:
- Do not make any changes until you have identified every mismatch.
- Do not redesign entities.
- Do not change the database schema.
- Do not change frontend code.
- Do not create approval DTOs.
- Do not modify unrelated files.
- Preserve all existing business logic and validation.
- Do not change existing backend status vocabulary.

FINAL APPROVED DTO CONTRACT

REQUEST DTOs:

1. PaymentBatchRequestDTO
- action: String, required
- batchId: Long, optional
- batchReference: String, required
- paymentType: String, required
- executionDate: LocalDate, required
- sourceAccountId: Long, optional
- sourceAccountName: String, optional transitional
- currencyCode: String, required
- totalAmount: BigDecimal, required
- description: String, optional
- payments: List<BatchPaymentRequestDTO>, required for submit and draft save with rows

2. BatchPaymentRequestDTO
- batchPaymentId: Long, optional
- employeeId: String, required
- employeeName: String, required
- beneficiaryAccount: String, required
- bankCode: String, required
- amount: BigDecimal, required

RESPONSE DTOs:

3. PaymentBatchResponseDTO
- batchId: Long
- batchReference: String
- paymentType: String
- status: String
- executionDate: LocalDate
- totalAmount: BigDecimal
- totalTransactions: Integer
- createdAt: LocalDateTime
- submittedAt: LocalDateTime, optional
- approvedAt: LocalDateTime, optional
- rejectedAt: LocalDateTime, optional
- processedAt: LocalDateTime, optional
- initialBalance: BigDecimal, optional
- finalBalance: BigDecimal, optional
- description: String, optional
- createdBy: SimpleUserDTO
- sourceAccount: SimpleAccountDTO
- currency: SimpleCurrencyDTO
- payments: List<BatchPaymentResponseDTO>

4. BatchPaymentResponseDTO
- batchPaymentId: Long
- employeeId: String
- employeeName: String
- beneficiaryAccount: String
- bankCode: String
- amount: BigDecimal

5. SimpleUserDTO
- userId: Long
- fullName: String

6. SimpleAccountDTO
- accountId: Long
- accountNumber: String, optional
- accountName: String

7. SimpleCurrencyDTO
- currencyCode: String
- currencyName: String, optional


CURRENT REQUIRED ARCHITECTURE:

Frontend Request DTO
        ↓
PaymentBatchController
        ↓
PaymentBatchService
        ↓
PaymentBatch / BatchPayment entities
        ↓
Repository

Repository
        ↓
Entities
        ↓
DTO mapping
        ↓
Response DTO
        ↓
Frontend

REVIEW THESE SPECIFICALLY:

1. Verify all 7 DTOs exist exactly once and their fields/types exactly match the approved contract.

2. Verify request DTOs are used for incoming controller requests.

3. Verify response DTOs are returned from controller endpoints instead of JPA entities.

4. Verify PaymentBatchService still preserves:
   - saveDraft
   - createAndSubmit
   - updateDraft
   - submitForApproval
   - loadBatchWithPayments

5. Verify existing validation and business logic were preserved.

6. Verify action is separate from persisted workflow status.

7. Verify backend status vocabulary remains:
   DRAFT
   PENDING
   APPROVED
   REJECTED
   PROCESSED

   Do NOT introduce SUBMITTED as a backend status.

8. Verify entity relationships are mapped correctly:
   - PaymentBatch.sourceAccount → SimpleAccountDTO
   - PaymentBatch.currency → SimpleCurrencyDTO
   - PaymentBatch.createdByUser → SimpleUserDTO
   - PaymentBatch.batchPayments → List<BatchPaymentResponseDTO>

9. Verify the DTO request fields correctly resolve the related entities:
   - sourceAccountId OR sourceAccountName → PaymentBatch.sourceAccount
   - currencyCode → PaymentBatch.currency

10. Pay special attention to UserRepository, AccountRepository and CurrencyRepository.
    Only keep repository changes if they are genuinely required for DTO/entity mapping.
    Do not add unnecessary repository methods or unrelated changes.

11. Do NOT create:
    - ApprovalQueueItemDTO
    - BatchApprovalActionRequestDTO
    - BatchApprovalResponseDTO

12. Do NOT modify:
    - PaymentBatch.java
    - BatchPayment.java
    - User.java
    - Account.java
    - Currency.java
    unless there is an actual compile-breaking issue directly caused by the DTO integration.

13. Compare the current implementation with the actual entity field names.
    If a field name differs from the approved contract, document the mapping rather than redesigning the entity.

14. Check for unnecessary changes and remove only changes that are clearly unrelated to the DTO implementation.

15. Finally run a Maven compile check.

Do NOT make speculative improvements.

After the review, report:

A. DTOs that exactly match the contract.
B. DTOs that need correction, if any.
C. Files currently modified.
D. Whether each modified repository file is actually necessary.
E. Exact entity-to-DTO and DTO-to-entity mapping decisions.
F. Whether controller endpoints return DTOs only.
G. Whether existing service/business logic is preserved.
H. Whether the project compiles successfully.
I. Any remaining issue that must be fixed before we proceed.

Only after presenting this review should you make corrections that are strictly necessary.



--------------------------------------------The DTO implementation changes have been kept and Maven compile reported:

COMPILE_STATUS=SUCCESS

Do NOT make any code changes yet.

I want a final post-implementation audit of the CURRENT working tree before I commit anything.

1. Run:
   git status --short
   git diff --stat
   git diff --name-status

2. Identify every changed, added, deleted, or untracked file.

3. Compare the current changes against the DTO task we just completed.

The intended implementation scope was:

CREATED:
- PaymentBatchRequestDTO.java
- BatchPaymentRequestDTO.java
- PaymentBatchResponseDTO.java
- BatchPaymentResponseDTO.java
- SimpleUserDTO.java
- SimpleAccountDTO.java
- SimpleCurrencyDTO.java

MODIFIED:
- PaymentBatchService.java
- PaymentBatchController.java

Only repository changes that are strictly required for the DTO mapping implementation are acceptable.

4. Pay special attention to:
- paymentdb.mv.db
- PaymentsService.java
- BatchPayment.java
- PaymentBatch.java
- any repository files
- any other files outside the DTO/controller/service scope

For every extra changed file, tell me:
- why it changed
- whether it is required
- whether it should be kept or reverted

5. DO NOT revert, delete, or modify anything automatically.
6. DO NOT make any new code changes.
7. DO NOT commit anything.
8. Do not change frontend code.
9. Do not create approval DTOs.
10. Do not redesign entities or database schema.

Finally report:

A. Intended files
B. Unexpected files
C. Files safe to revert
D. Files that must remain
E. Current compile status
F. Whether the working tree is clean enough to commit

If any file is ambiguous, stop and explain it rather than changing it.
