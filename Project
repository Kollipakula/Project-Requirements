Update only the Create Payroll frontend . Do not modify the payment currency implementation or any unrelated functionality.

Change the Debit Account field in Batch Details from a text input to a dropdown/select.
Since there is currently no backend/API for account data, use static frontend options:
Main Operating Account
Payroll Account
Foreign Currency (FX) Account
Keep the selected debit account in the existing frontend state/form handling so it works correctly when the form is submitted.
In Employee Transactions, keep the existing column order and add Bank Code immediately after Beneficiary Account and before Amount.
Bank Code should be a text input for now.
Change the Action column to provide both Edit and Remove functionality.
Edit should allow the selected employee transaction row to be modified without creating a duplicate row.
Remove should delete only the selected row.
Add Row should continue to work correctly with the new Bank Code field.
Update any frontend validation, state objects, form handling, and payload construction required for the new fields.
Do not introduce backend/API changes.
Do not change the existing Payment Currency implementation.
Do not change the existing navbar, sidebar, profile page, or other unrelated components.
Preserve the existing styling and make the new controls consistent with the current UI.
After making the changes, run the frontend build and report any errors.
For the Save and Submit for Processing buttons, do not create new button colors or styling. Reuse the existing common Button component/style from the project's common/shared folder (for example, Button.js) so that Create Payroll follows the application's standard button design, colors, hover states, spacing, and typography. Inspect the existing common Button implementation first and use it rather than duplicating its styles. Do not modify the common Button component unless absolutely necessary.


Revert any changes made to the common Button.js component and do not modify it.
Use the existing shared Button component exactly as it currently exists.
Only modify the Create Payroll frontend and its directly associated files.
Preserve the existing Payment Currency implementation.
Do not modify Navbar, Sidebar, User Profile, backend, or any other unrelated components.
After reverting the Button.js changes, run the frontend build again and confirm that it compiles successfully.





Fix only the horizontal overflow in the Create Payroll Employee Transactions section.

The new Bank Code column is correct and must remain after Beneficiary Account. Do not remove or change any existing functionality.

Make the Employee Transactions table and its input fields fit within the available desktop content width so that the page does not show a horizontal scrollbar at normal desktop resolution.

Adjust only the necessary Create Payroll table/layout CSS:

Make the columns responsive/flexible.
Reduce excessive column widths, padding, or gaps if necessary.
Ensure Employee ID, Employee Name, Beneficiary Account, Bank Code, Amount, and Action all fit within the available width.
Keep inputs visually consistent with the existing UI.
Keep Edit and Remove actions visible.
Do not change the Payment Currency implementation.
Do not modify Navbar, Sidebar, User Profile, backend, or unrelated components.
Do not change the overall page layout.
The goal is simply to make the existing Employee Transactions section fit cleanly without horizontal scrolling.

After the change, run the frontend build and report any errors.
